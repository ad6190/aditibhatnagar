# Technical Plan: Add a Footer with Copyright Year

**Issue:** #3
**PRD:** `docs/PRD.md`
**Spec:** `docs/spec/3.md`
**Date:** 2026-05-25
**Effort:** XS (< 1 day)

---

## Context

The blog is a React 19 SPA using React Router v7. All pages are wrapped by `Layout.tsx`, which renders a `<Navigation />` and an `<Outlet />`. A footer placed in `Layout.tsx` will appear on every page without any per-route changes.

**Current state of `Layout.tsx`:**
```tsx
import { Outlet } from "react-router-dom";
import { Navigation } from "./Navigation";

export function Layout() {
  return (
    <div className="min-h-screen flex flex-col">
      <Navigation />
      <main className="flex-1">
        <Outlet />
      </main>
      <footer className="border-t py-8 px-6 lg:px-8">
        <div className="mx-auto max-w-[1400px] flex justify-between items-center text-sm text-muted-foreground">
          <span>&copy; {new Date().getFullYear()} aditi.</span>
          <span className="text-xs">obsidian + react</span>
        </div>
      </footer>
    </div>
  );
}
```

**Note:** The footer already exists from a prior commit (visitor counter integration/removal). The implementation is already correct. The task is to verify it meets all spec criteria, ensure tests exist, and confirm build passes cleanly.

---

## Implementation Plan

### Step 1: Verify Current Implementation

1. Read `blog-site/site/src/components/Layout.tsx`
2. Confirm the `<footer>` element is present with:
   - `<footer>` semantic tag
   - `© {new Date().getFullYear()} aditi.` text
   - Responsive Tailwind classes
   - "obsidian + react" tagline

**Expected outcome:** Footer already meets all PRD acceptance criteria. No code changes needed to `Layout.tsx`.

### Step 2: Run Lint and Build

```bash
cd /Users/aditibhatnagar/Documents/temp/rohit-ai-sdlc-session/test-repo/blog-site/site
npm run lint
npm run build
```

Both must exit with code 0.

### Step 3: Write Tests

Create `blog-site/site/src/__tests__/Layout.test.tsx` with Vitest + @testing-library/react:

```tsx
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { describe, it, expect } from 'vitest'
import { Layout } from '../components/Layout'

describe('Layout footer', () => {
  it('renders a footer element', () => {
    render(<MemoryRouter><Layout /></MemoryRouter>)
    expect(screen.getByRole('contentinfo')).toBeInTheDocument()
  })

  it('displays copyright symbol and current year', () => {
    render(<MemoryRouter><Layout /></MemoryRouter>)
    const year = new Date().getFullYear().toString()
    expect(screen.getByRole('contentinfo')).toHaveTextContent(`© ${year} aditi.`)
  })

  it('displays the tagline', () => {
    render(<MemoryRouter><Layout /></MemoryRouter>)
    expect(screen.getByRole('contentinfo')).toHaveTextContent('obsidian + react')
  })
})
```

**Note on test infrastructure:** The project does not yet have Vitest configured. The test file will be written and the configuration will be scaffolded. If the runner cannot be fully wired up due to missing peer deps, the test file will be committed and noted as needing wiring.

### Step 4: Scaffold Vitest (if not already present)

Check `blog-site/site/package.json` for vitest. If missing:

```bash
cd blog-site/site
npm install --save-dev vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
```

Add to `package.json` scripts:
```json
"test": "vitest run",
"test:watch": "vitest"
```

Add `vite.config.ts` test config:
```ts
test: {
  globals: true,
  environment: 'jsdom',
  setupFiles: ['./src/test/setup.ts'],
}
```

Create `src/test/setup.ts`:
```ts
import '@testing-library/jest-dom'
```

### Step 5: Commit

```bash
git add docs/PRD.md docs/spec/3.md docs/plans/3.md
git add blog-site/site/src/__tests__/Layout.test.tsx
git add blog-site/site/src/test/setup.ts  # if created
git add blog-site/site/package.json blog-site/site/vite.config.ts  # if modified
git commit -m "feat: add footer with copyright year, PRD, plan, and tests

Adds copyright footer to Layout.tsx (already implemented), scaffolds Vitest,
and writes Layout footer tests.

Closes #3"
```

---

## Files to Create/Modify

| File | Action | Reason |
|------|--------|--------|
| `blog-site/site/src/components/Layout.tsx` | Verify (no change needed) | Footer already present |
| `blog-site/site/src/__tests__/Layout.test.tsx` | Create | Unit tests for footer |
| `blog-site/site/src/test/setup.ts` | Create (if needed) | Vitest + jest-dom setup |
| `blog-site/site/package.json` | Modify (if needed) | Add vitest deps + scripts |
| `blog-site/site/vite.config.ts` | Modify (if needed) | Add test config |
| `docs/PRD.md` | Create | Product requirements |
| `docs/spec/3.md` | Create | Feature spec |
| `docs/plans/3.md` | Create | This file |

---

## Risk Mitigation

| Risk | Mitigation |
|------|------------|
| Vitest deps conflict with React 19 | Use `--legacy-peer-deps` if needed (known issue with this repo) |
| Footer accidentally duplicated | Verify Layout.tsx first; do not add a second `<footer>` |
| Test infrastructure too heavy | Write test file, commit, note runner needs wiring |

---

## Definition of Done

- [ ] `Layout.tsx` has a `<footer>` with dynamic copyright year
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] `src/__tests__/Layout.test.tsx` exists with passing tests (or scaffolded with note)
- [ ] Commit message includes `Closes #3`
- [ ] docs/PRD.md, docs/spec/3.md, docs/plans/3.md all created
